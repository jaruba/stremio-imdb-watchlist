{
  "version": 2,
  "builds": [
    { "src": "*.js", "use": "@now/node" }
  ],
  "routes": [
    {
        "src": "/(?<list>[^/]*)/manifest.json",
        "dest": "/manifest.js"
    },
    {
        "src": "/(?<list>[^/]*)/(?<resource>[^/]*)/.*",
        "dest": "/$resource.js"
    }
  ]
}
